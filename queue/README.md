# WORKSHOP Queue

Canonical task definitions live under `tasks/`. Queue state is represented by task STATUS plus handoff/review evidence. Do not duplicate task bodies into multiple queue folders.

Use schemas/QUEUE_PROTOCOL.md and schemas/STATE_MACHINE.md.
