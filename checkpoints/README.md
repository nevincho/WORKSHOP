# WORKSHOP Checkpoints

Record each independently validated working point here using `schemas/CHECKPOINT_SCHEMA.md`.

A report cadence is not a checkpoint cadence. Create checkpoints after validated implementation changes, not merely every 8/12/24 hours.

Checkpoint records must provide sufficient repository/runtime references for rollback or identification of the known-good state.
